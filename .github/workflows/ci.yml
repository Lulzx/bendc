name: CI

on:
  push:
  pull_request:

jobs:
  bootstrap:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Install the official Bend (for base.bend, reference outputs and stage0)
        run: |
          curl -fsSL https://bend-lang.com/install.sh | BEND_NO_TELEMETRY=1 sh
          echo "$HOME/.bend/bin" >> "$GITHUB_PATH"

      - name: Install Bun (runs the JavaScript target's tests)
        run: |
          curl -fsSL https://bun.sh/install | bash
          echo "$HOME/.bun/bin" >> "$GITHUB_PATH"

      - name: Build bendc from the C seed
        run: make

      - name: Test suite
        run: make test

      - name: Seed is up to date (bendc.bend compiles to the seed)
        run: make selfcheck

      - name: bendc's checker accepts bendc itself, as the official one does
        run: |
          ./build/bendc --check-only "$HOME/.bend/bend2/base.bend" bendc.bend > build/ours.txt
          BEND_NO_TELEMETRY=1 bend bendc.bend --check-only > build/official.txt
          cmp build/ours.txt build/official.txt

      # The official bend peaks near 16 GB building bendc, the runner's RAM.
      - name: Add swap (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo fallocate -l 16G /swap2 && sudo chmod 600 /swap2 && sudo mkswap /swap2 && sudo swapon /swap2
          free -g

      - name: Full bootstrap (official bend -> stage1 -> stage2 fixpoint)
        run: BEND_NO_TELEMETRY=1 ./bootstrap.sh
